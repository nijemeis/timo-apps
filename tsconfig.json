{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  "//": "Tests run under Node, not React Native, so they are typechecked separately by `npm test` — with Node's types, which app code must not have.",
  "exclude": ["src/**/__tests__/**", ".test-build"]
}
